version: '3.3'

services:
    app:
        build: ./
        ports:
            - 8080:8080
        volumes:
            - .:/app
            - '/app/node_modules'
    database:
        image: mysql:5.7
        container_name: mysql
        restart: always
        environment:
            - MYSQL_PASSWORD=Suranith
            - MySQL_ROOT_PASSWORD=Suranjith6@
            - MYSQL_USER=root
            - DB_NAME=location
        ports:
            - '3366:3306'
        expose:
            - '3306'

      